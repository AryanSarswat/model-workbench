# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.env.local

# Private data (real dataset + generated DB) — only templates are committed
data/test_cases/
data/workbench.db
data/*.db

# Node / frontend (added when Phase 2 starts)
node_modules/
dist/

# Brainstorming companion scratch files
.superpowers/

# Working documents (dated specs, implementation plans) are not repo artifacts —
# only living docs (docs/*.md) and code are committed.
docs/superpowers/

# OS
.DS_Store
