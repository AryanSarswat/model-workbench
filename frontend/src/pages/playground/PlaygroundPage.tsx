import { PageHeader } from '../../components/PageHeader'

// Placeholder -- replaced by the Playground screen.
export default function PlaygroundPage() {
  return (
    <main style={{ padding: '40px 40px 0' }}>
      <PageHeader eyebrow="Chat" title="Playground" />
    </main>
  )
}
